// 2. MODULE
// avoids naming conflicts as it is it aplicable only to directory in it - not global and every .button on a global scale has a unique hash name

// import styles from './Button.module.css'

// function Button(){
//     return (
//         <button className={styles.button}>Click me</button>
//     )
// }

// export default Button